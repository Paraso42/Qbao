## v3.34.0
- **第 5 轮（test 账号实测：轮次入口/计数/历史配色/科目结构同步）**：
  - 侧栏进度错报修复：quizSets 的未作答空位经 JSON 持久化往返会变成 null，侧栏把 null 也计为「已答」（15 题的一轮显示 30/30 的根因）；现已与 -1/undefined 同等视为未作答（AppSidebar chapterAnswered），侧栏“已答/总题数”与进度条恢复真实值
  - 「没有答题入口却拦着不让出题」修复：出题守卫（ai.js）看“最后一轮未完成”而答题入口（StartView 快捷卡）看“当前轮指针”，多端合并/重复出题后指针停在已答完轮次时，最后一轮未完成会拦住出题却没有任何入口；现统一口径——新增 data store 的 hasUnfinishedQuizSet（任意一轮未完成即拦截，与服务端 in_progress 会话 409 语义一致）与 getActionableQuizSet（答题入口指向最新一轮未完成的轮次，全部完成回退最后一轮），快捷卡在进入答题/报告前先激活目标轮（activateQuizSet）
  - 答题历史配色：答对题目此前仅有极淡的描边（等于没有标注），与答错的红色背景不对等；答对题卡片改为与答错同权重的浅绿背景+描边
  - 科目章节合并灾难修复：mergeStates 旧实现把本地 subject 对象整体覆盖云端（含 chapterIds 数组），旧设备（本地骨架没拉到另一台设备新建的章节）一旦同步就会把新章节从科目中“刷没”，随后全量推送把云端科目结构也回退（实测：test 账号章节 2 被 15:06 的同步从 用户测试科目2 里抹除，已修复并云端恢复）；subjects 改为按 id 合并、本地优先（名称/折叠），chapterIds 取本地+云端并集，杜绝旧端回退科目结构（sync.js + 3 例回归单测）
  - 量表：data store 新增 3 个 K1 轮次守卫/可操作轮次 API（8 例单测）；同步合并并集 3 例回归；本地面板 161/161 用例、eslint 0 error
- **第 5 轮补充（部署后同源冒烟，生产验证）**：章节 1 侧栏 90/90 → 75/90（真实 75 已答/90 总）；章节 1 快捷卡由「查看报告」变为「开始刷题 共 15 题已答 0 题」且点开即定位最新未完成轮（体液调节特点轮）;章节 2 侧栏 30/30 → 15/30、快捷卡指向未完成的第 1 轮（生理学研究对象轮）；历史报告答对卡片浅绿背景、答错浅红背景同权重；云端科目 chapterIds 合并修复后旧端重新同步不再丢章节（章节 2 在侧栏恢复显示）


- **精简冗余功能（网页端测试第 2 轮结论）**：
  - 删除「错题本」标签页与错题 AI 讲解：/api/v1/ai/explain 接口、前端讲解逻辑与 wrongBook 缓存、相关测试整体移除；保留题库「仅显示错题」筛选、答题报告「只看错题」与错题标签/策略出题体系
  - 删除「间隔复习」（SRS）：科目页标签页、SM-2 结算（services/srs.js）、答题结算写入、srsData 持久化/同步链路、StartView 标语一并移除；历史数据留档不清理，已有 srs 类型旧考卷不再展示；大考卷功能不受影响
- **题库按轮次出题两级层级**：章节 →「第 N 轮出题 · 时间」（每次出题/导入 = 一轮，倒序展示，默认展开最新一轮）→ 题目；仅显示错题/搜索/批量编辑/导入导出保持在章节级，批量移动与删除同步维护轮次一致性
- **答题界面分享按钮**：从弹窗顶栏（与右上关闭键重叠）移到底部操作栏右下侧，与「我不会」同行，答前答后均可分享当前题目
- **网页端测试第 3 轮问题修复**：
  - 章节页统计卡移除「待巩固错题」一格（间隔复习已删除、无巩固区可待），统计网格自动适配两张卡片
  - 章节题量口径统一：有轮次（quizSets）的章节题量 = 各轮题目数之和（题库按轮次展示，同一道题在不同轮次按轮次计数）；无轮次的旧章节回退题库题数。修复「题库 45 题 vs 章节页题量 42 / 侧栏 45/42」显示不一致：章节页题量、侧栏 X/Y 与进度条、科目总览「当前题数」、大考卷章节题数全部采用同一口径（新增 services/chapterStats.js，5 例单测）
  - 积分实时结算：答题轮次完成（POST /quiz/session completed）响应新增 balance 字段，前端结算后立即同步本地余额并落盘；积分页每次打开再向服务端取一次权威余额，不再显示旧值
- **最终漏洞排查（第 4 轮：出题/答题刷新韧性 + 多端同步周密性）**：
  - 多端同轮并发答题答案不再互相覆盖：云端合并时同一轮次（quizSets 签名相同）的重复副本按题并集——已答补进空位，任何一侧进度不丢（services/sync.js mergeQuizSets）
  - 题库答案对齐：章节题库经多端去重合并后与轮次不再同序同长，此前按偏移写回会串题错位；新增 rebuildChapterAnswersFromSets 按题干把各轮答案重对齐到题库（合并后、结算回写、服务端会话恢复统一使用），章节正确率/错题统计不再漂移
  - 出题中途刷新不再静默停摆/重复扣费：未开始的任务刷新后自动续跑；服务端任务（serverTaskId）刷新后复用续跑不重复创建；本地直连且 AI 请求已在途的任务刷新后明确标记失败并提示「请重新开始出题」（避免未知是否已扣费时重复调用 AI）；新增 _wasRunning 刷新标记（migrateState）
  - 答完全部题目却因离线/断网/刷新未收到服务端结算确认 → 不再锁死「开始出题」：客户端「查看报告」、回到页面/恢复在线时自动补发最终结算（服务端增量结算幂等）；服务端出题守卫兜底——发现全答完的 in_progress 会话自动升级 completed 并按快照增量补结算（chapterSessionGuard）
  - 大考卷刷新丢进度修复：答题中的大考卷与当前轮次一样同步写本地活动会话键（qbao_active_exam），骨架/IDB 缺失时完整重建，已有考卷按题回填答案与进度
  - 页面销毁时把题目/考卷/历史大字段强制写一次 IndexedDB（pagehide 直接写，不再依赖空闲回调）
  - boot 时恢复当前章节服务端进行中的答题会话（此前仅在侧栏切换章节时恢复）；关闭页面前答题同步改 keepalive 请求；双击「查看报告」防重（本轮只结算一次、历史不重复）；服务端会话并入本地题库改为按题干去重补漏（不再整体覆盖）
  - AI 出题任务队列剪枝：已完成/失败任务保留最近 40 条（含大 promptText，此前无限累积会缓慢撑大 localStorage），排队/进行中任务永不清除
- **第 4 轮补充（test 账号实测回归）**：
  - 大考卷报告错报修复：大考卷结算后报告视图曾回落到「当前章节轮次」（currentExamId 过早清空导致 activeSet 失焦，题目对不上）；现结算后保留考卷上下文直到报告关闭，报告始终显示考卷题目
  - 大考卷列表完成态：答完的考卷按钮由「开始答题」变为「查看报告」（含完成标记与正确率），可随时重看该卷报告；答到一半关闭弹窗仍可继续
  - 出题中途刷新彻底不再卡队：云端拉取合并会把旧态（running/pending）回滚覆盖本地恢复/失败标记；aiTaskQueue 改为按 id 并集、同 id 本地优先，且每次云端合并后（onMerged 钩子）重新裁决队列——本地直连在途任务明确标记失败并提示重试、未开始任务自动续跑、服务端任务（serverTaskId）复用续跑直至完成
- **第 4 轮补充 2（test 账号实测 + 提示词系统检查）**：
  - 服务端任务队列历史显示：已完成（含已导入）的服务端出题任务此前会被移出列表，任务队列弹窗看不到任何历史出题；现保留为历史行（章节/题数/完成时间/已导入标记），手动导入后行不再消失，重复导入由幂等守卫拦截
  - AI 出题提示词系统检查修复：
    - 策略配额取整溢出：如 5 题+错题50%/复习50% 时旧算法得「-1 道新考点」；新增 computeStrategyTargets 顺序 clamp、总量守恒，提示词与策略符合度核算共用同一换算
    - 无错题/复习标签时的矛盾指令：旧提示词既要求出 error/review 题又说"暂无归入新考点"；现在出题前按标签实际情况把无标签类配额明确并入新考点（提示词同步说明），模型不再收到自相矛盾的数量与 strategy 要求
    - 增加防重复要求：同知识点必须变换问法/场景/数值，不得与资料示例或已出题目雷同
    - 服务端任务队列路径补上「已有学习进度/薄弱知识点」上下文（此前仅 /ai/generate 直连路径有，两条路径提示词行为不一致）——抽成共享 lib/chapterHistoryPrompt，两路径同一实现
    - 服务端默认提示词（worker/补题）与客户端字段要求对齐：补 id、输出顺序、防重复条款
  - 主观题（名词解释/简答）进入答题界面自动聚焦输入框：光标直接闪烁可输入，答题→回车提交→下一题继续，全键盘操作免一次鼠标点击
- 测试：app 19 文件 149 用例 / server 30 文件 152 用例全绿；eslint 0 error；三端版本对齐 3.34.0

## v3.33.1

- **修复网页端上线首轮测试 5 项问题**：
  - 设置→AI 配置：模型选择栏不再清空（Modal 关闭时销毁内容、父组件 pre-flush watch 在挂载前触发导致 loadForm 被跳过；改为组件挂载即回填，三种进入路径均恢复已保存模型）
  - 出题必失败修复：v3.32 拆分后 nonStreamGenerate 新增 deps 参数但调用点未传，deps.aiSelfCheck 抛 TypeError，客户端出题在上传资料后、发起请求前即失败（线上 aiTaskQueue 2 条 failed 任务实证；调用点补参数 + deps 默认值双保险）
  - 科目页「题库」「间隔复习」白屏修复：P2.2 重构时 shortText 函数体被误并入 rateColor、函数声明被删，模板 3 处调用渲染即 TypeError（48 个 vue 模板全量扫描复核无其他悬空引用）；题库分组首帧自动展开第一组（此前全折叠只显示标题）
  - AI 连接测试与讲解：上游瞬时故障（ECNU 网关 401「获取第三方元数据失败」类）自动重试一次并输出友好文案；实测生产环境 3 模型连测 6 次均 200，该 401 为网关瞬时抖动
- 测试：app 18 文件 118 用例全绿；server 154 用例全绿；eslint 0 error；三端版本对齐 3.33.1

## v3.33.0

- **错题本 + AI 讲解（P3.1）**：
  - 科目总览新增「错题本」页签：自动聚合历史答题中判错的题目，支持按章节过滤；每道错题展示我的答案/标准答案与作答时间
  - 一键 AI 讲解：服务端新增 POST /api/v1/ai/explain（非流式、zod 校验、复用 provider 工厂与 AI Key 请求头；成功才计费，与出题共享 AI 免费额度/超额扣分语义；失败/空返回不计费并返回 502 可读错误）
  - 讲解结果缓存于 wrongBook（按题目签名，上限 200 条 LRU，随本地状态持久化），可回看点开即看、一键重新生成
  - 服务端 8 例端点测试（成功计费/失败不计费/超额扣分/422/401/未知 Provider）；前端 2 例 store 测试
- **题库导入导出（P3.2）**：
  - 导入支持 .json 文件选择 / 直接粘贴，自动识别 JSON 数组与 CSV（表头 type,question,options,answer,tag,explanation，选项 | 分隔，answer 支持字母/数字索引，中文题型归一）；CSV 错误给出行号提示
  - 幂等去重：与当前题库按题目签名（题型+题干+答案+选项）去重，重复导入不产生重复题目（跳过数 toast 提示）
  - 导出当前章节为可再导入的 JSON（导出格式兼容再导入 round-trip）；纯逻辑抽为 services/importExport.js（8 例单测）
- **批量编辑（P3.3）**：题库页「批量」选择模式——勾选多题后支持移动到其他章节 / 批量删除（同步清理题库、轮次与 SRS 计划，历史保留）/ 一键设置标签
- **E2E 留档**：probe-p3-wrongbook.cjs（错题本渲染/AI 讲解按钮/导入导出批量入口）
- 测试：app 18 文件 118 用例（原 108）全绿；server 154 用例（146+8）全绿；eslint 0 error



- **store 拆分（P2.1）**：stores/ai.js 从 35.3KB 拆至 ~17KB——生成核心（题目归一化/策略符合度/非流式重试/池诊断/上传解析）迁至 services/aiTasks.js；服务端任务协调（列表/轮询续跑/导入/取消）迁至 services/aiServerTasks.js（组合式 controller）；章节资料管理迁至 services/aiMaterials.js；章节历史统计迁至 services/aiHistory.js（纯函数）。行为不变，新增拆分模块单测 8 例
- **巨型组件拆分（P2.2）**：
  - SettingsModal.vue（23.7KB → 15.6KB）：AI 配置区块 → AiConfigSection.vue
  - AdminTab.vue（29.6KB → 15.5KB）：公告管理 → AdminNoticesSection.vue、用户管理 → AdminUsersSection.vue
  - SubjectDashView.vue（33.7KB → 26.1KB）：科目总览 → SubjectOverviewPanel.vue
  - 拆分组件状态走 store，模板/样式随区块迁移，交互保持原地
- **聊天虚拟滚动（P2.4）**：ChatMessages.vue 窗口化渲染——按消息类型估算行高 + 前缀高度二分定位可视窗口，上下 spacer 撑开滚动区；1000 条消息实测仅渲染 ~11 个 DOM 节点（Electron 探针 probe-p2-chat-virtual.cjs 留档），顺带修复后台窗口 rAF 不触发导致的滚动位置不同步；窗口计算抽为纯函数 services/chatVirtual.js（4 例单测）
- **API 封装统一（P2.3）**：api.js 新增 apiFetch（统一 JSON/鉴权/FormData/401 语义）与 apiHandle（统一错误处理，替代 filesApi/pointsApi/usersApi 三份重复 _handle），readApiErrorSafe 统一错误读取（readApiError 保留别名）；aiApi.js 全部裸 fetch 收敛至 apiFetch；删除死代码 noticesApi.getAdminNotices；新增 6 例单测
- **发布断言（P2.5）**：release.yml 构建前断言 tag 与三端 package.json 版本一致；发布前三要素产物（exe/latest.yml/blockmap）存在性检查
- 测试：app 17 文件 108 用例（原 92）全绿；server 146 用例全绿；eslint 0 error



- **同步写收敛（P1.2）**：消除"无变化也全量 PUT"的写放大——
  - 空推跳过：引擎记录上次成功推送的脱敏序列化指纹，内容未变且 rev 基线未变时直接标记已同步（轮询/可见性/重复调度触发不再产生空 PUT）
  - 页面关闭 keepalive 推送接入同一空推检测，消除与进行中 flush 竞态导致的重复 PUT/409
  - 同步标记（pending / lastSync）按账号隔离（qbao_sync_pending_u_<uid>），防 A 账号遗留 pending 被 B 账号补推；旧全局键自动兼容迁移
  - resumePendingSync 返回 Promise 便于调用方串行等待；新增引擎级单测（启动门闩/多账号隔离/空推跳过/rev 预检合并/409 冲突自动重推）
- **密钥存储加固（P1.3）**：
  - 桌面端登录 token 与 AI Provider Key 改走主进程 safeStorage（Windows DPAPI）加密落盘（userData/secrets.json），renderer 不再持久化明文；新增 secret-save/load/remove IPC 与 preload 桥
  - 启动/登录时自动预热与迁移：旧 localStorage 明文一键转入安全存储后清除；safeStorage 不可用时自动降级（混淆存储，功能不中断）
  - 网页端最小混淆（XOR + 自实现 base64，中文安全），读取兼容旧明文；设置页明示两种形态的存储边界
- **store 层单测补位（P1.4）**：新增 quiz/chat/users/ai 四个 store 的核心状态流转单测（共 27 例），顺带修复三个缺陷：
  - data store getActiveSet 包装对象的 userAnswers 改为访问器绑定底层（此前"重开一轮"等整体替换不落持久层，刷新后旧答案回流）
  - 备份回档 restoreFromText 的科目/章节指针修正提前到 saveState 之前（此前落盘骨架指针为空，刷新丢失当前选中）
- **分层持久化 E2E 回归（P1.1）**：新增 tools/e2e/probe-p1-persistence.cjs 并在真实 Electron 构建产物上留档——
  2100 题题库启动不卡、刷新后活动会话续答（停在答 37/2100 的第 38 题）、答题后骨架仅 ~1.1KB（远低于 5MB 上限）、题目/答案全部分流 IndexedDB、多账号切换互不串号、登录态离线作答保留 pending 标记并落 IDB
- 测试：app 14 文件 92 用例（原 38）、server 146 用例全绿；两端 eslint 0 error



- **ESLint 静态检查接入**（P0.4）：app/server 两端 eslint 配置（入口基线规则集：未定义变量/重复声明为错误，样式类为警告留痕；浏览器/Node 全局内联维护，零运行时依赖）；lint 脚本用 npx 固定 eslint@9.39.5（两端 0 error）；CI 后端/前端 job 各增 lint 步骤
- **替换原生 prompt/alert**（P0.5）：桌面端「修改服务器地址」改为应用内输入框 + toast 反馈（复用 ui.openPrompt），services/api.js 移除 showServerSetupDialog；全仓无 window.prompt/alert 残留
- **renderMarkdown 占位符伪造防护**（P0.6）：公式占位符改用每次渲染随机盐，用户输入伪造占位符不再被误还原为 undefined 或错位 KaTeX HTML；escapeHtml 增加无 DOM 环境回退（纯逻辑可单测）——新增 4 例单测
- **AI 配额计数/扣费原子化**（P0.7）：checkAndChargeAiQuota 在真实连接池上包事务 + 同用户 advisory 锁（hashtextextended），并发请求串行裁决，杜绝先计数后扣费的双请求蹭免费额度（TOCTOU）；测试基建 installFakePool 默认 stub pool.connect，带事务的服务调用封闭可测——新增 3 例单测
- **AI 上传配额失败清理落盘文件**（P0.8）：/ai/upload 配额/校验抛错时删除 multer 已落盘文件，防磁盘残留——新增路由级测试
- **E2E 回归资产入库**（P0.3）：.tmp 的 Electron UI 探针/QA 脚本收编至 tools/e2e/（含用法文档与样例报告），输出统一进 tools/e2e/out/（gitignore）；含密钥的 local/e2e-ai-scenarios 保持本地不提交
- **桌面升级链路核验**（P0.2）：v3.30.1 Release 资产与 latest.yml 实测一致（sha512/size/url 完整）；实机升级冒烟留待人工（记入 local/log.md）
- 测试：server 29 文件 146 用例、app 7 文件 38 用例全绿；两端 eslint 0 error

## v3.30.1


- 出题流程调整（按用户验收反馈）：
  - **停用流式输出**：流式分支整体注释保留（可恢复），一律改为"生成完成 → 校验 → 一次性导入"，保证 AI 二次校准能在导入前生效、出题质量可控
  - **AI 自动判定（二次校准）开关**：设置页「AI 自动判定」Toggle 由用户自行开启/关闭（开启后生成时对客观题做一次额外校验调用）
  - **移除「我会了」**：答题界面不再提供"我会了"跳过入口（ignoreCurrent/isIgnored 逻辑同步删除，ignoredQuestions 数据字段保留兼容）；「我不会」保留；主观题判定保持不变
- 文件池**一份文件可关联多个章节**（user_files_chapters 关联表，迁移 011_v3.30_user_file_chapters.sql）：
  - assign 追加关联、不再顶掉原章节；unassign 解除全部关联；列表按关联表过滤
  - AI 出题（/ai/generate 与后台任务队列共用 lib/poolText.js）按关联表读取章节资料
- 修复：后台任务出题与材料不匹配（任务队列未合并文件池资料 → 空资料生成无关题目）、
  服务端任务完成后误入本地任务区、重复导入产生重复轮次、合并去重后 currentQuizSetIdx 越界
- 修复：答题入口崩溃（TypeError: reading 'length'，QuizView idx/模板/题号点防御补全、
  getActiveSet/getCurrentQuizSet 越界回退、createQuizSetForChapter 按题目签名去重）
- 其他：新增 favicon.svg（消除 /favicon.ico 404）

## v3.30.0

- **本地持久化分层（性能整改）**：localStorage 只存轻量骨架（章节元数据/策略/配置），
  题目/答案/大考卷/SRS/历史等大字段入 IndexedDB（stateDb.js），空闲时全量写入不阻塞交互——
  2000+ 题题库不再超 localStorage 5MB 上限、不再全量序列化卡顿
- **答题进度可靠恢复**：活动会话键（qbao_active_session）每次保存同步写入，刷新/关闭后
  恢复答题入口与进度；null→undefined 语义统一（未答题目不再被计为已完成）；启动同步门闩
  防止骨架态覆盖云端数据
- **多账号隔离**：登录态只读写账号专属存储键，杜绝账号间数据串号
- 服务端任务队列修复：刷新不重复创建任务、导入幂等（importedServerTaskIds）、
  服务端任务自动轮询续跑、本地/服务端任务分区显示
- 测试：persistence 7 用例（骨架化/活动会话/null 语义/账号隔离），app 34/34、server 142/142

## v3.29.0

- **积分系统上线**（激活此前预留的 `storage_points`/`points_extended` 空间）：
  - 赚取：注册 +20、每日首次登录 +5、答对客观题 +1/题（每日上限 30，轮次完成按增量结算，重复同步不乱发）、成就解锁 +10~+200（按档位，服务端幂等领取）、分享题库被他人下载 +2/次（单库封顶 +20，匿名/自己下载不计）
  - 消耗：文件池续期 10 积分/7 天（事务内扣分+延长，余额不足 400）；AI 出题每日免费 10 次、超出 2 积分/次（入口预扣）；AI 文件解析每日免费 10 次、超出 1 积分/次
  - 台账：`points_ledger` 表记录每一笔变动（余额快照 + 幂等唯一键），用户中心新增「积分」页（余额/规则/明细分页），文件页移除「功能预留」文案并支持真实验证续期
  - 管理员：「积分管理」专区（查用户余额/台账、±调整必填原因）、用户列表/详情显示积分
  - 学期清零：每年 2/1 与 8/1 00:00 清零并记账（advisory lock 防多实例重复，进程内定时器），清零前 7 天前端警示
  - 防滥用：AI 任务每用户同时排队上限 3（防占满串行 worker）、AI 出题/解析按日免费额度、答题每日上限、一次性事件唯一约束、每日 03:00 余额对账（台账为准）
  - 服务端：`config/points.js` 集中全部规则数值，可按运营调整；迁移 `server/sql/migration_v3.29_points.sql`（`node scripts/run_migration.js sql/migration_v3.29_points.sql`）

## v3.28.4

- 双端（桌面端/网页端）数据同步全面修复：
  - 根因 1（网页端启动不拉取）：旧逻辑仅在本地无缓存时才从云端恢复，网页端本地有旧缓存时永远不会拉取云端的桌面端新数据——"桌面端出完题，打开网页端看不到"。现在启动时总是拉取云端并与本地合并（实体级并集，任一侧数据都不丢）
  - 根因 2（旧状态覆盖云端）：启动时会拿到最新云端 rev 但保留本地旧状态，网页端任何操作触发推送都会以"匹配的最新 rev"把旧状态直接覆盖云端（桌面端新题被冲掉，数据丢失）。现在推送前先轻量校验云端版本（`GET /api/v1/data/rev`），云端有新写入则先拉取合并再推送
  - 根因 3（无主动拉取）：新增 20s 轮询 + 页面焦点/可见性恢复时即时拉取，桌面端出题后网页端 ≤20s 自动出现；本地未推送内容也会在轮询/离屏时补推
  - 服务器新增轻量 `GET /api/v1/data/rev` 接口（返回 rev/synced_at），轮询零全量流量；旧服务器自动降级为全量拉取合并
  - 新增合并策略测试：旧缓存拉云端新题、推送前合并防覆盖

## v3.28.3

- 头像裁剪所见即所得（WYSIWYG）修复：
  - 修复裁剪导出区域与预览不一致：原导出算法按「视口中心」推算源矩形，图片放大后导出区域相对显示内容整体上移/左移，贴住上/左边界时实际头像会包含图片上方/左方的空白（JPEG 转黑）。现改为按「视口左上角」直接映射图片自然坐标，任意缩放与拖动位置下导出内容与预览完全一致
  - 新增纯函数 `avatarCropSource()` 并配套单元测试（贴边无空白、导出中心=视口中心映射、任意缩放下恒等）
- 聊天消息头像统一圆形：消息列表内双方头像由圆角矩形（--radius-md）改为正圆（border-radius: 50%），与好友列表/会话列表/申请列表/顶栏/侧栏等全站一致，不再露出圆形裁剪头像的黑角

## v3.28.2

- 头像交互与反馈优化：
  - 修复裁剪弹窗拖动/缩放的边缘问题：图片偏移上限改为 0（不再允许拖出视口露出空白），缩放下限动态锁定为「图片恰好铺满裁剪框」（cover 后最小 100%），并新增操作提示文案
  - 上传成功即时反馈：裁剪确认后 toast「头像已更新，各界面已同步显示」，顶栏/侧栏/用户中心等通过响应式 watch 即时刷新；头像 URL 追加时间戳防浏览器缓存旧图
  - 上传失败/异常兜底提示：canvas 处理失败、图片读取失败、格式/大小不符均给出明确 toast；上传中按钮显示「上传中…」防重复提交
  - 清理测试数据：删除误创建的冒烟账号 avatarsmoke_msyuxpmp 及其头像文件

## v3.28.1

- 头像功能修复（上传后无法显示）：
  - 根因 1（生产 nginx）：静态资源正则 `location ~* \.(png|jpg|…)$` 优先级高于 `/avatars/`、`/uploads/` 前缀代理，所有头像/图片 URL 经 nginx 一律 404。已将两处前缀 location 改为 `^~` 并更新部署文档
  - 根因 2（URL 相对路径）：服务端返回 `avatars/<id>.jpg` 相对地址，桌面版（file://）与带路径页面下 `<img>` 无法加载。新增 `resolveMediaUrl()` 统一将相对媒体 URL 解析为绝对 URL（网页版取当前 origin，桌面版取 API_BASE origin），覆盖全部头像渲染点
  - 头像显示：顶栏用户按钮、侧栏用户区、用户中心档案、账号页预览、好友会话列表、好友列表、聊天消息（含发送/接收双方）、好友申请列表、管理员用户列表与用户详情
  - 附带修复：聊天图片与文件 URL 同样经 `resolveMediaUrl` 解析（同一 nginx 根因）

## v3.27.4

- 开关（Toggle）修复：
  - 修复设置页内所有开关轨道被撑宽（原 108px → 恢复 36px）：设置行 `.settings-row label` 的 min-width 误作用于开关根元素（label），导致轨道被拉伸、圆点位移看起来“卡在半路”。现在开关自身锁定 min/max-width 与轨道一致，设置行规则排除开关元素
  - 修复开关圆点垂直居中偏差（原偏下 1px）：圆点 top 按 轨道高 − 左右边框 − 圆点直径 精确居中

## v3.27.3

- 聊天/好友与文件池、UI 细节修复：