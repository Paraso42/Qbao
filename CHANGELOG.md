## v3.29.0

- **积分系统上线**（激活此前预留的 `storage_points`/`points_extended` 空间）：
  - 赚取：注册 +20、每日首次登录 +5、答对客观题 +1/题（每日上限 30，轮次完成按增量结算，重复同步不乱发）、成就解锁 +10~+200（按档位，服务端幂等领取）、分享题库被他人下载 +2/次（单库封顶 +20，匿名/自己下载不计）
  - 消耗：文件池续期 10 积分/7 天（事务内扣分+延长，余额不足 400）；AI 出题每日免费 10 次、超出 2 积分/次（入口预扣）；AI 文件解析每日免费 10 次、超出 1 积分/次
  - 台账：`points_ledger` 表记录每一笔变动（余额快照 + 幂等唯一键），用户中心新增「积分」页（余额/规则/明细分页），文件页移除「功能预留」文案并支持真实验证续期
  - 管理员：「积分管理」专区（查用户余额/台账、±调整必填原因）、用户列表/详情显示积分
  - 学期清零：每年 2/1 与 8/1 00:00 清零并记账（advisory lock 防多实例重复，进程内定时器），清零前 7 天前端警示
  - 防滥用：AI 任务每用户同时排队上限 3（防占满串行 worker）、AI 出题/解析按日免费额度、答题每日上限、一次性事件唯一约束、每日 03:00 余额对账（台账为准）
  - 服务端：`config/points.js` 集中全部规则数值，可按运营调整；迁移 `server/sql/migration_v3.29_points.sql`（`node scripts/run_migration.js sql/migration_v3.29_points.sql`）

## v3.28.4

- 双端（桌面端/网页端）数据同步全面修复：
  - 根因 1（网页端启动不拉取）：旧逻辑仅在本地无缓存时才从云端恢复，网页端本地有旧缓存时永远不会拉取云端的桌面端新数据——"桌面端出完题，打开网页端看不到"。现在启动时总是拉取云端并与本地合并（实体级并集，任一侧数据都不丢）
  - 根因 2（旧状态覆盖云端）：启动时会拿到最新云端 rev 但保留本地旧状态，网页端任何操作触发推送都会以"匹配的最新 rev"把旧状态直接覆盖云端（桌面端新题被冲掉，数据丢失）。现在推送前先轻量校验云端版本（`GET /api/v1/data/rev`），云端有新写入则先拉取合并再推送
  - 根因 3（无主动拉取）：新增 20s 轮询 + 页面焦点/可见性恢复时即时拉取，桌面端出题后网页端 ≤20s 自动出现；本地未推送内容也会在轮询/离屏时补推
  - 服务器新增轻量 `GET /api/v1/data/rev` 接口（返回 rev/synced_at），轮询零全量流量；旧服务器自动降级为全量拉取合并
  - 新增合并策略测试：旧缓存拉云端新题、推送前合并防覆盖

## v3.28.3

- 头像裁剪所见即所得（WYSIWYG）修复：
  - 修复裁剪导出区域与预览不一致：原导出算法按「视口中心」推算源矩形，图片放大后导出区域相对显示内容整体上移/左移，贴住上/左边界时实际头像会包含图片上方/左方的空白（JPEG 转黑）。现改为按「视口左上角」直接映射图片自然坐标，任意缩放与拖动位置下导出内容与预览完全一致
  - 新增纯函数 `avatarCropSource()` 并配套单元测试（贴边无空白、导出中心=视口中心映射、任意缩放下恒等）
- 聊天消息头像统一圆形：消息列表内双方头像由圆角矩形（--radius-md）改为正圆（border-radius: 50%），与好友列表/会话列表/申请列表/顶栏/侧栏等全站一致，不再露出圆形裁剪头像的黑角

## v3.28.2

- 头像交互与反馈优化：
  - 修复裁剪弹窗拖动/缩放的边缘问题：图片偏移上限改为 0（不再允许拖出视口露出空白），缩放下限动态锁定为「图片恰好铺满裁剪框」（cover 后最小 100%），并新增操作提示文案
  - 上传成功即时反馈：裁剪确认后 toast「头像已更新，各界面已同步显示」，顶栏/侧栏/用户中心等通过响应式 watch 即时刷新；头像 URL 追加时间戳防浏览器缓存旧图
  - 上传失败/异常兜底提示：canvas 处理失败、图片读取失败、格式/大小不符均给出明确 toast；上传中按钮显示「上传中…」防重复提交
  - 清理测试数据：删除误创建的冒烟账号 avatarsmoke_msyuxpmp 及其头像文件

## v3.28.1

- 头像功能修复（上传后无法显示）：
  - 根因 1（生产 nginx）：静态资源正则 `location ~* \.(png|jpg|…)$` 优先级高于 `/avatars/`、`/uploads/` 前缀代理，所有头像/图片 URL 经 nginx 一律 404。已将两处前缀 location 改为 `^~` 并更新部署文档
  - 根因 2（URL 相对路径）：服务端返回 `avatars/<id>.jpg` 相对地址，桌面版（file://）与带路径页面下 `<img>` 无法加载。新增 `resolveMediaUrl()` 统一将相对媒体 URL 解析为绝对 URL（网页版取当前 origin，桌面版取 API_BASE origin），覆盖全部头像渲染点
  - 头像显示：顶栏用户按钮、侧栏用户区、用户中心档案、账号页预览、好友会话列表、好友列表、聊天消息（含发送/接收双方）、好友申请列表、管理员用户列表与用户详情
  - 附带修复：聊天图片与文件 URL 同样经 `resolveMediaUrl` 解析（同一 nginx 根因）

## v3.27.4

- 开关（Toggle）修复：
  - 修复设置页内所有开关轨道被撑宽（原 108px → 恢复 36px）：设置行 `.settings-row label` 的 min-width 误作用于开关根元素（label），导致轨道被拉伸、圆点位移看起来“卡在半路”。现在开关自身锁定 min/max-width 与轨道一致，设置行规则排除开关元素
  - 修复开关圆点垂直居中偏差（原偏下 1px）：圆点 top 按 轨道高 − 左右边框 − 圆点直径 精确居中

## v3.27.3

- 聊天/好友与文件池、UI 细节修复：