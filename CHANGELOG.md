## v3.36.0
- **手机端竖屏交互工程化（小屏幕交互专项，采纳用户反馈修订）**：
  - **主操作单槽位状态机**：章节工作台「开始出题」与「开始答题」共用同一按钮槽位按状态切换——无轮次 →「开始出题」（资料/登录/队列/未完成轮次等原因灰化并显示）；出题中 → 同槽位灰化「出题中…」；出好且未答完 → 同槽位变「开始答题」；本轮答完 → 回到「开始出题」（可生成下一轮）；失败/取消自动回落。三端（手机/网页/桌面）同源生效，主按钮 1 屏内可达，不再需要滚到页面底部找「开始出题」（services/startActions.js 纯函数 + 8 例单测）
  - **移除「查看报告」按钮及其章节轮次入口**：章节轮次答完不再弹「本轮报告」，关闭并提示「可到答题历史查看回顾」（答题历史已覆盖答对/答错/正确率/逐题/只看错题/搜索）；大考卷的报告流程保持不变；结算补发由 setFinished 监视器后台触发（幂等），移除入口不丢失服务端结算
  - **登录门禁：抛弃「未登录/离线」状态（采纳用户决策：最简单彻底的隔离）**：未登录整页只渲染登录/注册门禁（LoginGate），不渲染任何业务功能；拒绝匿名存储——saveState/persistMergedState 在无账号时一律不落盘（公共键 quizEngineState_v7 永久停用，loadState 未登录一律空态启动、不读公共键）；匿名期内存如有改动（saveState 被拒打标 hadAnonymousMutations），后续任何登录（含同账号）都强制整页重建，杜绝匿名改动混入账号；移除 AuthDialog/「继续以离线模式使用」入口；门禁保留设置入口（桌面端首次运行需先配置服务器地址）；每个账号只读写自己的账号键/IDB 分区/云端，互不串扰（+2 例：匿名拒读/拒写、匿名改动锁）
  - **账户隔离加固（根治多账号串账：A 账号题库合并进 B 账号）**：根因 = 同一浏览器会话内直接切换账号（登录/注册）不会重建数据上下文——内存里上一账号的题库被继续写入新账号的本地键并推上新账号云端。修复（四层）：① 账号切换（applyAuth 检测数据属主 owner 变化）先冻结同步引擎再整页重建（新账号自己的骨架键/IDB 分区/云端）；② 同步引擎账号守卫（canSync：切换冻结期 + 属主漂移一律停摆，覆盖轮询/可见性/卸载补推等全部入口）；③ IndexedDB 大字段按账号分区（行键 uid 前缀；登录态绝不回退旧全局行——旧 global/misc 行是跨账号混杂数据，历史/考卷由云端恢复；章节旧无前缀行仅当当前账号骨架引用其 cid 时采纳）；④ 活动会话/考卷 localStorage 镜像键按账号后缀（读优先账号键，旧全局键仅升级回退且由骨架引用守卫过滤）；20s 轮询 pending/lastSync 键已按账号（v3.31）。E2E 实测：test→新号 X（建题作答）→切回 test，双向本地与云端均无串账（X 空态启动、X 云端仅自身数据、test 云端/本地不含 X 任何实体）；临时验证账号云端已清空。存量串账数据需人工确认后清理
  - **存储配额治理（根治手机端「本地保存失败：存储空间已满」连续报错）**：根因 = 每次保存都同步写 localStorage 的活动会话/考卷镜像键（2000+ 题一轮可达数 MB），撑爆 iOS ~5MB 总量后骨架键写不进 → 每次答题都报错；修复 = 镜像迁 IndexedDB（复用 qbao_state_db global store，无需 DB 版本迁移），≤1MB 小会话保留 localStorage 影子保「刷新/关闭必达」，超预算大会话只写 IDB；hydrate 影子优先 → IDB 回退；历史遗留超预算大键读取后自动清理；QuotaExceeded 自动清理镜像键重试一次（成功改提示「已自动清理临时缓存」）；致命告警 30s 冷却消除连发（persistence/stateDb + 6 例单测）
  - **专业移动交互细节**：≤768px 输入控件一律 ≥16px（iOS 聚焦不再自动缩放）；交互元素 touch-action:manipulation（双击缩放/长按选词误触消除）；coarse 指针触控目标 ≥40px（题号点/工具栏/数字步进/标签热区等）；中文 IME 组合期（isComposing/keyCode 229）回车不误发送/不误提交（聊天 + 主观题 + 全屏键盘快捷键）；答题任务操作栏 sticky 贴底（长题时按钮始终可见、safe-area 兜底）；题号导航横滑替代换行堆叠；出题策略卡可折叠（手机窄屏默认收起，桌面默认展开）；标签管理 coarse 指针下显示显式重命名按钮（桌面保留双击）；科目总览滑杆拇指 28px；弹窗 ≤480 紧凑内边距 + 操作按钮全宽均分 + 键盘弹起顶部对齐；hero 与统计卡窄屏紧凑化；/dl 落地页 ≤640 单列卡片化（td[data-label] 前缀、下载按钮全宽 48px 高）
  - 文档：docs/MOBILE_UX.md（移动交互规范 + 真机验收清单）、DEVELOPMENT_FLOW.md DoD 新增「手机竖屏 ≤768 检查」行
  - 量表：app 21→26 文件 177→203/203（startActions 8、persistence 配额治理 6 + 账户隔离 4 + 登录门禁拒读拒写 2、user store 切换/门禁重建 4、sync 引擎守卫 2）、server 36 文件 183/183（/dl 移动 CSS 断言并入既有用例）、scripts 6、desktop 5、三端 eslint 0 error
## v3.35.0
- **下载安装体系工程化重构（manifest-first 统一分发/更新平台，模仿行业成熟项目）**：
  - 服务器储藏室升级为清单驱动：downloads/manifest.json（schemaVersion 1；stable/beta 双渠道、多版本留存、sha256/sha512/大小/日期/更新说明/required 强制门槛/retracted 撤回标记），旧 meta.json 废弃；新增端点：/api/v1/desktop/manifest（版本清单）、/api/v1/desktop/download?file=<文件名>（任意留存版本精确下载，断点续传）、/api/v1/desktop/update/<channel>/latest.yml 与 exe/blockmap（electron-updater 自托管更新源）、/api/v1/desktop/stats（下载统计，013 迁移）、公开落地页 /dl（中国大陆镜像下载站，动态渲染版本历史/校验值/撤回横幅）；/api/v1/desktop/latest 保持兼容；/download 短链语义不变