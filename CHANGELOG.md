## v3.27.3

- 聊天/好友与文件池、UI 细节修复：
  - 修复“发送失败: Expected object, received null（file_info）”：发送消息时不提交空的 file_info/quiz_data/reply_to 字段（服务端 schema 同步允许 nullable）
  - 修复设置页滑块滑条过长、滑块实际行程不符（自定义轨道按拇指宽度内缩，长度=实际行程）
  - 章节内答题历史取消跨章节切换下拉（当前章节即对象，保留返回主页）
  - 科目题库题块削减高度、整行可点击；修复大考卷“选择章节”因 v-if/v-for 同元素导致章节列表不渲染（自动捕获科目章节）
  - 文件池上传对齐后端白名单（仅 pdf/doc/docx/pptx/txt/md），并收起与后端不一致的图片类型
  - 文件池过期文件不再残留在章节复习资料区（拉取文件池时同步清理失效引用）
  - 好友入口回归顶栏（侧栏“好友消息”移除）；侧栏移除“答题历史”单独入口；AI 任务并入“AI 出题”一行（开关打开时可点击查看任务队列）
  - 修复标签“展开全部 N 个”误代入全部标签名称（改为真实个数）
  - 个性化字体设置对顶栏与主页实际生效（按容器缩放 --fs-* 字号体系），题目字体大小亦接入答题区
  - 修复通用开关（Toggle）轨道过长、圆点开启位移与轨道宽度脱节（改为按 轨道内容宽度 − 圆点直径 − 两侧内边距 通过 CSS 变量与 calc() 计算，开启时圆点精确停靠轨道右端内边缘，关闭回到左端；位移不再写死数值）

## v3.27.2